#!/bin/sh -e

export DESTDIR="$1"

./configure \
    --prefix=/

make
make install

# Remove HTML documentation.
rm -rf "$1/share/doc"

# Compress manpages
find "$1/share/man" -type f | xargs gzip -n -9
