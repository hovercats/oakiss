#!/bin/sh -e

./configure \
    --prefix=/pkg \
    --sysconfdir=/etc \
    --mandir=/pkg/share/man \
    --localstatedir=/var

make
make DESTDIR="$1" install

# Compress manpages
find "$1/pkg/share/man" -type f | xargs gzip -n -9
