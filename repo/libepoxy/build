#!/bin/sh -e

export DESTDIR="$1"

# Disable building tests.
: > test/meson.build

meson setup \
    -Dprefix=/pkg \
    -Dsysconfdir=/etc \
    -Dmandir=/pkg/share/man \
    -Dpkg_config_path=/pkg/lib/pkgconfig \
    -Dx11=true \
    output

samu -C output
samu -C output install
