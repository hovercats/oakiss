#!/bin/sh -e

export DESTDIR="$1"

muon setup \
	-Dprefix=/usr \
	-Dbuildtype=release \
	-Ddoc=disabled \
	-Dtests=disabled \
	-Db_colorout=never \
	-Ddefault_library=both \
	output

samu -C output
muon -C output install
