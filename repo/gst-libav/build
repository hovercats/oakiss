#!/bin/sh -e

export DESTDIR="$1"

muon setup \
	-Dprefix=/usr \
	-Dbuildtype=release \
	-Ddoc=disabled \
	-Dtests=disabled \
	-Db_colorout=never \
	-Ddefault_library=both \
	build

samu -C build
muon -C build install
