#!/bin/sh -e

./configure \
    --prefix=/ \
    --enable-shared \
    --enable-static \
    --disable-rpath

make
make DESTDIR="$1" install

# Compress manpages
find "$1/share/man" -type f | xargs gzip -n -9
