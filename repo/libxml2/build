#!/bin/sh -e

./configure \
    --prefix=/pkg \
    --with-threads \
    --with-history \
    --without-python \
    --without-icu

make
make DESTDIR="$1" install

# Compress manpages
find "$1/pkg/share/man" -type f | xargs gzip -n -9
